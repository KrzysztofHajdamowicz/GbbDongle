# GbbDongle for the Waveshare ESP32-S3-POE-ETH-8DI-8DO, WiFi variant.
# Dashboard-adoption import target (see gbbdongle.yaml for details);
# published images are built from the -factory and -ha variants.
# Build: uv run esphome compile firmware/gbbdongle-8di8do-wifi.yaml

substitutions:
  device_name: gbbdongle-8di8do-wifi
  friendly_name: GbbDongle (Waveshare ESP32-S3-POE-ETH-8DI-8DO, WiFi)

packages:
  base: !include common/base.yaml
  wifi: !include common/wifi.yaml
  board: !include boards/waveshare-esp32-s3-poe-eth-8di-8do.yaml
  board_wifi: !include boards/waveshare-esp32-s3-poe-eth-8di-8do-wifi.yaml
