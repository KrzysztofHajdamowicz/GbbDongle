# Dev overlay: verbose logging. WiFi dev variants additionally include
# common/wifi-dev.yaml to join your WiFi from secrets.yaml.

substitutions:
  log_level: VERBOSE
  # Dev builds always compile the working-tree component, not GitHub.
  external_components_source: ../components
