# Dev overlay: verbose logging, joins your WiFi from secrets.yaml
# (copy secrets.yaml.example to firmware/secrets.yaml first).

substitutions:
  log_level: VERBOSE

wifi:
  networks:
    - ssid: !secret wifi_ssid
      password: !secret wifi_password
