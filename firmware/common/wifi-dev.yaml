# Dev overlay for WiFi boards: joins your WiFi from secrets.yaml
# (copy secrets.yaml.example to firmware/secrets.yaml first).

wifi:
  networks:
    - ssid: !secret wifi_ssid
      password: !secret wifi_password
